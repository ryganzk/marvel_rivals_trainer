# Marvel Rivals API Key
# Get your API key from https://marvelrivalsapi.com/
MARVEL_RIVALS_API_KEY=your_api_key_here
MARVEL_RIVALS_API_VERSION=v1

# HMR Configuration for binding to 0.0.0.0
# When accessing via WSL IP or network, uncomment and set to your actual IP
# NEXT_PUBLIC_VERCEL_URL=localhost:3000

# Force Fast Refresh to use polling (already in package.json but belt-and-suspenders)
WATCHPACK_POLLING=true
CHOKIDAR_USEPOLLING=1
